// Intentionally empty.
//
// Showtime listing and the seat map belong to build stage 2/3, alongside the
// reserve/confirm endpoints. Adding read queries here now would mean guessing
// the shape of data the booking flow has not defined yet.
export {};
